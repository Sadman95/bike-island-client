import dashboard from './dashboard';
import orders from './orders';
import other from './other';
import products from './products';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, products, orders, other]
};

export default menuItems;
